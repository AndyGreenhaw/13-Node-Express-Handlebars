<h1>The Burger App</h1>

<img src ="/assets/burger-illustration-andy-custom.png" alt="burger-image">

<h2>Add a Burger and Devour It</h2>

<div class="formController">
<form class="create-form">
  <div class="form-group">
    <label for="burgerInput"></label>
    <input type="text" id="burgerInput" name="burger_name" placeholder="Burger Name">
  </div>
  <button class="main-button" type="submit">Add Burger</button>
</form>
</div>

<table>
    <tr>
      {{#each burgers}}
        {{> burgers/burger-block}}
      {{/each}}
    </tr>
</table>

{{!-- <ul>
  {{#each burgers}}
    {{> burgers/burger-block}}
  {{/each}}
</ul> --}}